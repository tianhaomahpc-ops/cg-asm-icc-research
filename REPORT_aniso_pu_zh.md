# σ 张量轴:overlap 反常的对比度依赖,与两个新的单位分解

> **⚠ 结论已被取代。** 本报告的数字来自 2D numpy 谐振器。在真实 MFEM/PETSc/MPI 3D 栈上重测后,
> 其中六条结论不成立(对比度翻转、sASM 变负担、1.85× 增益、δ-ramp 解饱和、粗空间 3.8×、ω 相关性)。
> 请以 [`REPORT_aniso_mfem_zh.md`](./REPORT_aniso_mfem_zh.md) 为准 —— 那里有逐条撤回清单(第 6 节)。
> 本文件保留作为 2D 方法学与谐振器的记录。

本报告补上此前整套实验缺失的一根轴。`asm_demo.cpp` 到上一个提交为止一直是
`ConstantCoefficient sigma(1.0)`——Sys1/Sys2/Sys3 与全 Dirichlet 四个变体**全部是各向同性标量系数**。
而心脏组织的 σ 是张量。本报告回答三个问题:

1. "对比倍数小的时候 overlap 增加、迭代数上升"——**这个现象在张量下如何随对比度变化?**
2. 现在用的 **multiplicity scale(sASM,$1/\sqrt{m_k}$)在张量下还成立吗?**
3. 能否**由观测设计出更好的权重**?

**一句话结论**:反常是一种**低对比度病**;它的驱动因子**不是**子域解的不精确度 $\omega$
(实测相关系数 $+0.08$),而是 **overlap 对 $\lambda_{\min}$ 的回报率**(相关系数 $-0.92$);
而 $1/\sqrt{m_k}$ 这个**纯几何**权重在高对比度下会**反过来变成负担**(比不加权的 BASIC 还慢),
因为它掐住的正是 $\lambda_{\min}$。替换成 **σ-调和单位分解**后,单层最多快 **1.85×**,
粗基复用后再快 **2.3×**。

---

## 0. 方法与可复现性

两套实现:

| | 位置 | 用途 | 本报告数据来源 |
|---|---|---|---|
| **自带谐振器** | `asm_bug_demo/aniso/` | numpy/scipy + 自写 IC(0) C 内核,无 MFEM/PETSc/MPI 依赖 | **全部数字** |
| **生产实现** | `asm_demo.cpp`、`twolevel_aniso.c` | MFEM/PETSc,跑真实规模 | 代码已写、语法已验,**待你本机 `make`** |

> **诚实声明**:本机(容器)没有 MFEM / PETSc / MPI,因此 `asm_demo.cpp` 的改动与
> `twolevel_aniso.c` **没有被编译运行过**。它们经过了 mock 头文件下的
> `gcc/g++ -Wall -Wextra -fsyntax-only` 检查(该检查抓出并修掉了 4 个真实编译错误:
> C 的三元运算符不是左值 ×2、`-std=c11` 下 `M_PI` 未定义 ×2)。
> **本报告的每一个数字都来自自带谐振器**,不是来自 MFEM 端。

### 谐振器的设定(对齐 `coloring.c` / `twolevel.c` 的 Fig 8/9 方法学)

- **算子**:$-\nabla\cdot(\sigma\nabla u)=1$,$[0,1]^2$,P1 三角元,$n_x=120$($N=14520$);
  $x=0$ 面 Dirichlet + 其余 Neumann(Sys3 形状)。解析自检:$\sigma=I$ 时 $u=x-x^2/2$,
  实测 $\max u=0.500069$、相对误差 $2.1\times10^{-5}$(P1 离散误差量级)。
- **张量**:$\sigma=\sigma_t I+(\sigma_l-\sigma_t)ff^\top$,对比度 $r=\sigma_l/\sigma_t$,
  **det 归一**($\sigma_l=\sqrt r,\ \sigma_t=1/\sqrt r$)——保证扫的是**对比度**而不是整体扩散强度。
  纤维方向扫 $0°$(与字典序 DOF 编号对齐)与 $45°$(不对齐)。
- **分解**:$6\times6=36$ 个盒状子域;overlap 用矩阵图 BFS 逐层生长(PETSc `MatIncreaseOverlap` 语义)。
- **子域解**:自写 IC(0),**与 `-sub_pc_type icc -sub_pc_factor_levels 0` 语义一致**——
  因子稀疏结构严格等于 `tril(A)`(零填充),非正主元时按 PETSc 的
  `MAT_SHIFT_POSITIVE_DEFINITE` 重试对角平移。另备精确 Cholesky 作 $\omega=1$ 对照。
- **谱**:CG-Lanczos 三对角的 Ritz 值(即 `KSPComputeExtremeSingularValues` 的做法)。

### 三个正确性锚点(`make -C asm_bug_demo/aniso sanity`)

| 锚点 | 结果 |
|---|---|
| $O=0\Rightarrow D=I$,五种权重必须全同 | basic/flat/graded/ramp/harm **全部 150 步**,$\lambda$ 逐位相同 ✓ |
| graded $q=1$ 必须恒等于 flat(sASM) | $O=1,2,3$:129/125/123 **逐位相同** ✓ |
| 反常必须复现 | BASIC $150\to165\to204\to214\to220\to218$ ↑;sASM ↓;精确 Cholesky $72\to29$ ↓ ✓ |

第三行与你 Sys3($n_x{=}48$,4 ranks)的 $132\to148\to179\to199\to203\to202$ 形状一致。

---

## 1. 图 A:反常是一种**低对比度病**(`figA_contrast_mechanism.png`)

反常幅度定义为 $\mathrm{iter}(O{=}3)/\mathrm{iter}(O{=}0)$,BASIC + ICC(0):

| $r$ | 1 | 2 | 5 | 10 | 20 | 50 | 100 |
|---|---|---|---|---|---|---|---|
| **纤维 0°** | **1.39** | 1.37 | 1.43 | 1.31 | 1.13 | 1.07 | 0.99 |
| **纤维 45°** | **1.39** | 1.33 | 1.16 | 0.97 | 0.81 | 0.64 | **0.57** |

**你的观测被完整证实**:对比倍数越小,反常越重;大到某个阈值($45°$ 下 $r\approx10$、
$0°$ 下 $r\approx100$)反常自动消失,overlap 恢复经典 Schwarz 行为。

绝对迭代数($45°$,$L=0$):

```
r=1     BASIC  190 → 205 → 249 → 264 → 264 → 260     (O=0..5)  ↑↑ 病得最重
r=10    BASIC  181 → 173 → 176 → 176 → 172 → 168               ≈ 临界
r=100   BASIC  291 → 218 → 183 → 166 → 153 → 143               ↓↓ 完全健康
```

---

## 2. 机理:我上一轮的假设被**证伪**了

我此前推断 σ 通过 $\lambda_{\max}\le\omega\hat N$ 里的 $\omega=\lambda_{\max}(M_i^{-1}A_i)$ 进入。
直接测 $\omega$:

| $r$ | 1 | 2 | 5 | 10 | 20 | 50 | 100 |
|---|---|---|---|---|---|---|---|
| $\omega$,纤维 45° | 1.218 | 1.207 | 1.213 | 1.214 | 1.215 | 1.215 | 1.215 |
| $\omega$,纤维 0° | 1.218 | 1.259 | 1.374 | 1.469 | 1.555 | 1.630 | 1.634 |

- $45°$:$\omega$ **完全平**,而反常幅度从 1.39 扫到 0.57 ⇒ $\omega$ **解释不了**。
- $0°$:$\omega$ 反而**单调上升**,与反常幅度**反相关** ⇒ 更不是它。

**皮尔逊相关(两个角度合并,14 个数据点):**

$$\text{iter 比} \ \text{vs}\ \lambda_{\max}\text{放大}: \mathbf{+0.908}\qquad
\text{vs}\ \lambda_{\min}\text{放大}: \mathbf{-0.921}\qquad
\text{vs}\ \omega: \mathbf{+0.080}$$

### 真正的机理:一场**赛跑**

把 $O{=}0\to O{=}3$ 的两个因子分开测:

| | $\lambda_{\max}$ 惩罚(重复计数) | $\lambda_{\min}$ 回报(overlap) | $\kappa$ 净变化 |
|---|---|---|---|
| $r=1$(45°) | $\times2.73$ | $\times1.46$ | $\times1.87$ **变坏** |
| $r=10$ | $\times2.44$ | $\times2.36$ | $\times1.04$ 打平 |
| $r=100$ | $\times2.07$ | $\times$**6.22** | $\times0.33$ **变好** |

$\lambda_{\max}$ 的惩罚($\approx\hat N$)**对 σ 几乎免疫**(2.73→2.07);
$\lambda_{\min}$ 的回报却随对比度**暴涨**(1.46→6.22)。

**物理解释**:overlap 买的是"信息跨越子域边界的能力"。在强各向异性介质里,
$\delta$ 层 overlap 沿纤维方向的**能量传播距离** $\sim\delta\sqrt{\sigma_l}$,远大于几何距离;
而在各向同性介质里 $\delta$ 层就只值 $\delta h$,对 $H$ 而言微不足道。
**所以低对比度下 overlap 的重复计数账单照付、$\lambda_{\min}$ 的好处却收不到——这就是反常。**

> **对你上一轮回复的订正**:我说"σ 只从 $\omega$ 这个口子进来"是错的。
> σ 进的是 $C_0^2/\lambda_{\min}$ 这个口子。**好消息是提出的修法不变**——
> σ-调和 PoU 恰恰是冲着 $\int\sigma|\nabla\chi|^2$(即 $C_0^2$)去的;
> 但**理由必须改**,而且它改变了"最优修法"的排序(见 §5)。

---

## 3. 图 B:$1/\sqrt{m_k}$ 在张量下会**变成负担**(`figB_pou_families.png`)

五个权重族,全部写成同一个对称 $\sqrt{\text{PU}}$ 形式(即你 §12 验证过的结构):

$$M^{-1}=\sum_i R_i^\top W_i\,\tilde A_i^{-1}\,W_i R_i,\qquad \sum_i w_i(k)^2=1$$

| 记号 | $w_i$(未归一) | 说明 |
|---|---|---|
| `basic` | $1$,不归一 | `PC_ASM_BASIC` |
| `flat` | $1$ | $\Rightarrow 1/\sqrt{m_k}$,**= 你现在的 sASM** |
| `graded` | $q^{\text{depth}}$ | = `scheme 7 -pugrade`,你现有最优权重 |
| **`ramp`** | $\max(0,\,1-\text{depth}/(\delta+1))$ | **NEW-A**,$|\nabla\chi|\sim1/\delta$ |
| **`harm`** | σ-调和延拓 | **NEW-B**,$\int\sigma|\nabla\chi|^2$ 的极小元 |

**$r=100$,纤维 45°,ICC(0) 迭代数:**

| $O$ | 1 | 2 | 3 | 4 | 5 | 6 | 8 |
|---|---|---|---|---|---|---|---|
| basic | 218 | 183 | 166 | 153 | 143 | 136 | **123** |
| **flat(你现在)** | 200 | 177 | **168** | **160** | **155** | **154** | **155** |
| graded $q0.8$ | 189 | 152 | 132 | 117 | 108 | 102 | 94 |
| **ramp(NEW-A)** | 206 | 160 | 136 | 119 | 107 | 98 | 87 |
| **harm(NEW-B)** | 198 | 154 | 134 | 118 | 106 | **97** | **84** |
| 精确 Cholesky(下界) | 196 | 151 | 124 | 105 | 92 | 81 | 65 |

**三条结论:**

1. **$O\ge3$ 起 sASM 比完全不加权的 BASIC 还慢**(168 vs 166,155 vs 123)。
   $\lambda_{\max}(\text{sASM})$ 不再被压在 $\omega\approx1.21$,而是**随 overlap 爬升**
   $1.95\to2.00\to2.13\to2.22\to2.24$——$1/\sqrt{m_k}$ 是各向同性的尺子,
   在各向异性算子上**沿纤维掐得太狠、横向掐得不够**,$\sqrt{\text{PU}}$ 的封顶性质失效。
   **这是 multiplicity scaling 在张量问题上的一个真实失效模式,你现有六份报告里没有。**
2. **harm 比 flat 快 1.85×**(84 vs 155),且**比 BASIC 还快 1.46×**,距精确 Cholesky 下界仅 1.29×。
3. **$\sigma=I$ 时 harm ≡ ramp 逐位相同**($r=1$:133/142/142/141)——这是设计 B 该有的退化锚点 ✓。

**$r=1$(你的痛点),纤维 45°:** $O=4$ 时 flat 155 / graded 138 / ramp 133 / harm 133。
**只有 14%。** 我上一轮说"可能白捡 2×"——**没有兑现,应予订正**。
原因见 §2:低对比度下 $\lambda_{\min}$ 本来就不吃 overlap,把 PoU 变光滑也榨不出多少。
低对比度的解药在下一节。

---

## 4. 图 C:粗空间,以及**经典 $1/m_k$ 粗基在张量下的崩塌**(`figC_scaling.png`)

固定配置($O=3$,36 子域):

| $r$ | 细 PoU | 粗基 | iter | $\lambda_{\min}$ | 相对你现状 |
|---|---|---|---|---|---|
| **1** | flat | 无 | **156** | 2.72e-04 | (现状) |
| 1 | flat | $1/m_k$(= `twolevel.c`) | 46 | 3.36e-02 | 3.4× |
| 1 | **harm** | **harm(复用)** | **41** | 3.57e-02 | **3.8×** |
| **100** | flat | 无 | **168** | 3.21e-04 | (现状) |
| 100 | flat | $1/m_k$ | **159** | 2.29e-03 | **1.06× ← 几乎没用** |
| 100 | **harm** | **harm(复用)** | **113** | 6.41e-03 | **1.49×** |

**弱可扩展性**(子域尺寸固定 $20\times20$,$O=2$,扫子域数 $4\to36$):

| | $r=1$,45° | $r=100$,45° |
|---|---|---|
| 单层 sASM(flat) | $54\to157$,**2.91×** | $50\to177$,3.54× |
| 单层 harm | $52\to150$,2.88× | $44\to154$,3.50× |
| 两层 classical(flat/$1/m_k$) | $39\to45$,**1.15×** ✓ | $51\to171$,**3.35× ✗ 崩** |
| 两层复用(harm/harm) | $36\to40$,**1.11×** ✓ | $45\to138$,3.07× ✗ 仍崩 |
| **两层富化(harm × {1,s,t})** | $24\to20$,**0.83×** ✓✓ | $39\to69$,**1.77×** 大幅改善 |

**结论:**

1. **低对比度下粗空间是压倒性的杠杆**(3.4–3.8×,且迭代数与子域数无关)。
   这与你 Fig 9 的结论一致,本报告只是在张量轴上确认它。
2. **高对比度下经典 Nicolaides $1/m_k$ 粗基基本失效**(51→171,3.35×,与单层无异)。
   它是从**网格**造的,不含算子信息。
3. **粗基复用是免费的**:同一个 $\chi_i$ 既做细层夹心又做粗基,setup 不增加;
   它把高对比度下的粗空间从"没用"救回"有用"(159→113),但**仍不可扩展**。
4. **每子域 3 个基向量的 MsFEM 式富化**($\chi_i\times\{1,s,t\}$,$(s,t)$ 为纤维对齐局部坐标)
   把 $r=1$ 压到 **20 步且随规模下降**,$r=100$ 的增长率从 3.07× 降到 1.77×。
   要在高对比度下真正做平,还是得上 **GenEO**(局部广义特征问题)——
   而 GenEO 的特征问题里用的权重矩阵**正好也是这个 $\chi_i$**,所以 NEW-B 付一次赚三次。

---

## 5. 按对比度给的选型(取代我上一轮那张表)

| 你的 $r$ | 首选 | 迭代增益(实测) | 代价 |
|---|---|---|---|
| **$r\lesssim5$(反常最重)** | **两层 + PoU 复用**,细层 harm | **3.8×**(156→41),且可扩展 | 粗空间 = 子域数维,极小 |
| | 只改权重(ramp/harm) | 仅 1.14× | 一行 / 一次局部解 |
| $5\lesssim r\lesssim20$ | 两层复用(harm/harm) | 2.5×(123→50) | 1 向量/子域;富化未在此 $r$ 实测 |
| **$r\gtrsim50$** | **细层 harm**(权重就值 1.85×)+ 富化粗基 | 单层 1.85×,两层 1.49× | 同上 |
| $r\gtrsim50$ 且要可扩展 | **GenEO**(Nicolaides 系已崩) | — | 局部特征问题,setup 贵 |

**对 multiplicity scale 的最终评价:**
它在 $\hat N$ 那一项上是精确且剂量正确的,$\sigma$ 近各向同性时仍是单层对称路线的合理基线;
但它是**从网格造的、与算子无关的**权重,因此
(a) 在低对比度下够用但天花板很低(权重只值 ~14%,真正的杠杆是粗空间);
(b) 在高对比度下**会变成净负担**(比 BASIC 还慢)。
这两条都指向同一个替换:把 PoU 的度量从**图距离**换成 **σ-能量距离**。

---

## 6. 代码

### 自带谐振器(已跑,可立即复现)

```bash
cd asm_bug_demo/aniso
make sanity      # 三个正确性锚点
make figs        # 五个扫描 + 三张图(约 70 s)
```

- `ic0kernel.c` — IC(0) 分解 + 三角解 + 批量子域 apply(ctypes)
- `anisopu.py`  — 算子、分解、五种 PoU、粗基(含富化)、CG-Lanczos
- `fig1..fig5_*.py`, `plot_all.py`

### 生产实现(已写、语法已验,**待你 `make`**)

**`asm_demo.cpp`** 新增:

| 开关 | 含义 |
|---|---|
| `-aniso r` | 对比度 $r=\sigma_l/\sigma_t$;默认 det 归一,`-aniso_raw` 改为 $\sigma_t=1$ |
| `-fiber ax,ay,az` | 纤维方向(内部归一),默认 `1,0,0` |
| `-scheme 7 -puramp` | **NEW-A** δ-归一 ramp 权重 |
| `-scheme 7 -puharm` | **NEW-B** σ-调和权重(setup 期每子域一次 COMM_SELF CG+ICC(0),**每步零加价**) |

`-aniso` 对 `-dt>0`(Sys1)与纯刚度(Sys2/Sys3)两条装配路径都生效。
`-puramp`/`-puharm` 复用了 `-pugrade` 已有的 BFS 层深与
**精确全局重归一 $\sum_i w_i^2=1$** 代码路径(`asm_demo.cpp` 中 `BuildHarmonicPU` 与其下游)。

```bash
make && ./collect_aniso.sh 48 4 1,1,0        # r ∈ {1,2,5,10,20,50,100} × O ∈ {0..3}
                                             #   × {basic, sASM, ramp, harm}
```

**`twolevel_aniso.c`**(新文件,不动你验证过的 `twolevel.c`):P1 FEM 各向异性算子 +
三种粗基 `-coarse mult|harm|enrich`。

```bash
make twolevel_aniso
./twolevel_aniso -aniso 100 -fiber_deg 45            # 固定配置,四种方法对比
./twolevel_aniso -aniso 100 -fiber_deg 45 -scale     # 弱可扩展性扫描
```

### 交叉校验建议(按你仓库一贯的严格度)

谐振器与 MFEM 端**几何/离散不同**(2D P1 vs 3D P1 tet,盒状分区 vs METIS),
所以不该期待逐位一致。可比的是**三条趋势**:
(1) 反常幅度随 $r$ 单调下降并在某个 $r^\*$ 穿过 1;
(2) `harm` 与 `flat` 的差距随 $r$ 单调拉大,且在某个 $r$ 之后 `flat` 劣于 `basic`;
(3) `-aniso 1` 时 `-puharm` 与 `-puramp` 迭代数应当相同(退化锚点),
且 `-pugrade 1` 应当仍然逐位等于 scheme 3。
第 (3) 条是纯粹的自检,**先跑它**。

---

## 7. 未做 / 待定

- MFEM/PETSc 端**未编译未运行**(本机无工具链)。
- 谐振器是 **2D**;3D 下 $H/\delta$ 与 $\hat N$ 都更大,反常的绝对幅度可能不同,
  方向性结论(对比度依赖、flat 的失效、粗基崩塌)预期不变但**未验证**。
- GenEO 未实现,只是从 $r=100$ 的可扩展性崩塌推断它是必需的。
- 未做时间(wall-clock)口径对比。`-puharm` 的 setup 多一次每子域局部 CG,
  在心脏"矩阵固定、每时间步复用预条件"的口径下应被完全摊销,但**未实测**。
- 纤维角只扫了 $0°$/$45°$ 两个;真实心脏纤维是逐点旋转的场,
  `-fiber` 目前是常张量,做逐点 `MatrixFunctionCoefficient` 是直接的下一步。
