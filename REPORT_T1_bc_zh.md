# Task 1:边界条件对可解释性的影响(全 Dirichlet / 全 Neumann / 混合)

## 0. 问题定义(先写清楚,具体是哪个例子)

**连续问题**:在**单位几何**——单位超立方体 $\Omega=[0,1]^d$($d=1,2,3$)上解 Poisson/Laplace 问题
$$-\Delta u=f\ \text{in}\ \Omega,\qquad \text{边界条件三选一(见下)}.$$

**离散问题**:标准 $(2d{+}1)$ 点有限差分 Laplacian,每轴 $n$ 个节点
(本文 1D $n{=}256$、2D $n{=}64$、3D $n{=}24$;即与之前 Dirichlet 最小算例同规模)。

**三套边界条件(本任务的三个具体例子)**:
- **(D) 全 Dirichlet**:$u=0$ 于**所有面**。SPD、非奇异。**这就是之前的 1D/2D 最小算例**(确认:`Laplace2D` 的 5 点模板在边界自然截断 = 各面齐次 Dirichlet)。
- **(N) 全 Neumann**:$\partial u/\partial n=0$ 于**所有面**。**奇异**,零空间 = 常数(行和为 0)。
- **(M) 混合**:**1 面 Dirichlet + $(2d{-}1)$ 面 Neumann**。即之前 3D **Sys3** 型(1 Dir + 5 Neu)。

**预条件**:$P{\times}P({\times}P)$ 盒式重叠加性 Schwarz(2D $P{=}4$、3D $P{=}4$、1D 8 子域),overlap $O{=}2$;
**BASIC vs sASM**,**精确(Cholesky)/ 不精确(ICC(0))** 子域解;可选 **Nicolaides 单位分解两层粗空间**。
外层 **CG** 到 $\|r\|/\|b\|<10^{-8}$。**奇异(全 Neumann)用常数 `MatNullSpace` 去均值**(一致 RHS),
粗算子 $A_0=R_0AR_0^\top$ 同样继承常数零空间、用带零空间的 CG 解。

**程序**:`asm_bug_demo/bc_suite.c`;数据 `bc_suite.txt`;图 `fig_bc_compare.png`。

---

## 1. 逐项对比(按现有逻辑,9 个 case = 3 维 × 3 BC)

> 说明:之前给的自由度是"逐项对比,或增减你认为需要的"。**与 BC 无关的项**(过度计数、不精确机制)
> 在三套 BC 下**逐位相同**(数据见下),把它们各渲染一遍不增加信息;所以这里用**逐项对比表 + 关键差异图**,
> 而不是把 13 张图 ×6 个 BC 重画 78 遍。每一项都标注"BC 是否改变它"。

| 可解释性项 | 全 Dirichlet | 全 Neumann | 混合 | BC 改变它吗? |
|---|---|---|---|---|
| **过度计数 $\hat N$** | 2 / 4 / 8 | 2 / 4 / 8 | 2 / 4 / 8 | **否(几何量)** |
| **$\lambda_{\max}$(BASIC,精确)** | =$\hat N$(2/4/8) | =$\hat N$ | =$\hat N$ | **否**(= $\hat N$ 逐位成立) |
| **$\lambda_{\max}$(sASM,ICC)=$\omega$** | 1D 2.42 / 2D 1.26 / 3D 1.21 | ≈同左 | ≈同左 | **几乎不**(子域不精确度 BC 无关) |
| **反常(overlap↑→iter↑)** | 有(需 ICC,2D/3D) | 有,且**更重** | 有,且更重 | **强度变**(见下) |
| **BASIC-ICC 迭代(2D)** | 102 | **157** | **157** | **是**(Neu/混合更难) |
| **sASM-ICC 迭代(2D)** | 67 | 100 | 99 | 是 |
| **$\lambda_{\min}$(sASM-ICC,2D,单层)** | 0.0071 | 0.0039 | **0.0010** | **是(关键)** |
| **两层迭代(2D)** | 51 | 53 | 47 | **否(被抹平)** |
| **$\lambda_{\min}$(两层,2D)** | 0.045 | 0.053 | 0.043 | **否(被抹平)** |
| **奇异性** | 非奇异 | **奇异(常数零模)** | 勉强非奇异(1 面钉住) | **是** |

### 逐项说明

**(i) 过度计数 $\hat N$ —— BC 无关(几何量)。** $\hat N=\max_k m_k$ 只由"子域怎么切、overlap 多大"决定,
与边界条件无关。实测三套 BC 下 $\hat N$ 逐位相同(2/4/8),且 **$\lambda_{\max}(\text{BASIC,精确})=\hat N$ 在每套 BC 下都精确成立**。
⟹ **sASM 的"去 $\hat N$"机制是 BC 无关的**:它在三套 BC 下都把 $\lambda_{\max}$ 压回 $\omega$。

**(ii) 不精确 $\omega$ —— 几乎 BC 无关。** $\omega=\lambda_{\max}(\text{sASM,ICC})$ 由子域 ICC 的不精确度决定;
子域块即使在全 Neumann 下仍带"人工 Dirichlet 切割面",所以不精确度几乎不随全局 BC 变(实测 2D 都 ≈1.26)。

**(iii) 反常 + sASM 收益 —— 机制 BC 无关,但被 BC 的低端放大。** "overlap↑→iter↑"和"sASM 修复"在三套 BC 下都出现(2D/3D);
但 **Neumann/混合 的单层迭代更多**(2D BASIC-ICC 157 vs Dirichlet 102),因为它们的低端($\lambda_{\min}$)更糟(见 iv)。
sASM 在三套 BC 都有效(去 $\hat N$),但它**不修 $\lambda_{\min}$**,所以单层 sASM 仍带 BC 差异(67/100/99)。

**(iv) $\lambda_{\min}$ / 全局模 —— 这才是 BC 真正改变的东西。**
- **全 Dirichlet**:低端最好($\lambda_{\min}$ 最大,2D 0.0071),全局模被四面 Dirichlet 压住。
- **全 Neumann**:**奇异**,常数是**精确零模**;去掉常数后,最小非零 $\lambda_{\min}$ 也小(2D 0.0039)——
  那个"几乎常数"的光滑模就是信息传播的**慢全局模**。
- **混合**:只有 1 个 Dirichlet 面把常数勉强钉住 → $\lambda_{\min}$ **最小**(2D 0.0010,接近奇异)→ 单层最难。

**(v) 两层粗空间 —— 把 BC 差异抹平(本任务的核心结论)。**
单层迭代带 BC 差异(2D sASM 67/100/99);**两层后变成 51/53/47,几乎相同**;
$\lambda_{\min}$ 从 (0.0071/0.0039/0.0010) **都升到 ≈0.045** 并**对齐**。
原因:**BC 改变的是全局/常数模,而 Nicolaides 单位分解粗空间($\sum_i\phi_i=1$)恰好张成常数** ——
它正是被 BC 扰动的那个模。所以**粗空间把"BC 的影响"整片吃掉**。

**(vi) 全 Neumann 的特别之处 —— 粗空间是"完美"的。**
全 Neumann 的零空间 = 常数 = Nicolaides 粗空间的精确成员($\sum_i\phi_i=1$)。
所以**两层 sASM 对全 Neumann 尤其干净**:粗层精确捕获那个零模(奇异性),细层 sASM 去过度计数 ——
两层把奇异问题变成与 Dirichlet 同样好条件(2D 两层 Neu 53 ≈ Dir 51)。

---

## 2. 一句话结论

> **过度计数 $\hat N$ 是几何量,与边界条件无关**(三套 BC 下 $\lambda_{\max}(\text{BASIC,精确})=\hat N$ 逐位相同),
> 所以 **sASM 去 $\hat N$ 的整套机制在全 Dirichlet / 全 Neumann / 混合 下完全一样**。
> **边界条件唯一改变的是低端 $\lambda_{\min}$(全局/常数模)**:全 Neumann 奇异(常数零模)、混合勉强钉住(最小 $\lambda_{\min}$)、全 Dirichlet 最好;
> 这导致单层迭代带 BC 差异(2D 67/100/99)。**两层粗空间(Nicolaides 张成常数)恰好吃掉这个被 BC 扰动的模**,
> 把三套 BC 的 $\lambda_{\min}$ 和迭代都**抹平对齐**(2D 两层 51/53/47)。对**全 Neumann 这个粗空间是精确的**(= 零空间)。

**图**:`fig_bc_compare.png` —— (a) $\lambda_{\max}(\text{BASIC,精确})=\hat N$ 三套 BC 重合;
(b)(c) 2D/3D 迭代:单层有 BC 差异、两层抹平。
