# 同一个滑窗修法在 Sys1 / Sys3 上成立吗

> 承接 `REPORT_fischer_vm_zh.md`(Sys2)。测试台 `fischer_sys13_test.c`:三个系统同网格、
> 同一个 Vm(t) 驱动、每步一次干净冷启动做基准。数据 `fischer_sys13_regime{0,2}.csv`,
> 图 `fig_fischer_sys13.png`。补丁 `fischer_sliding_window.patch` 现在**同时改 Sys2 和 Sys3**。

---

## 0. 结论

- **Sys3:成立,而且是三个系统里收益最大的。** 现行 `-fischer3` 在 regime 0 是 **±0%**(实际比冷启动多 19 次迭代),修好窗口 **−76.3%**;regime 2 是 −69.2% → **−98.2%**。
  **⇒「Sys3 初值类封顶 ~10%」是坏淘汰规则的产物,不是物理上限。**
- **Sys1:也成立**(−10.9% → −62.8% / −67.9% → −98.6%),但 Sys1 只占三系统总迭代的 **~5%**,而且 **warm 单独就有 −27.8%(免费)**。而且现在的停机判据用的是 `rtol`,**不改的话任何初值都省不了迭代**。
- **窗口大小:各自定,但都很小**(§5 的 m 扫描,扣掉维护开销后)。Sys1 **m=4**、Sys2 **m=4–12**、Sys3 **m=8–12**。
  也就是说 **`F3MAX=12` 正好、`FISCH_MAX=16` 只大了一点点 —— 窗口大小并没有配错,错的是淘汰规则**;Sys1 则该加一个 4 向量的小窗口。
  (这也更正我之前说的"Sys3 的窗口大小才是旋钮":加大窗口一律无收益,Sys3 的优势来自它的轨迹本来就只有 9 维。)

---

## 1. 测试台

| | 算子 | 右端 | 性质 | cold 迭代/步 |
|---|---|---|---|---|
| **Sys1** | `A₁ = c·I + ½K_σm`(`c=χCm/Δt`,调到 cold≈16 步,对齐真机的 15) | 人造成使**精确解就是 Vm(t)** | SPD、质量主导 | 16 |
| **Sys2** | `K_{σi+σe}` | `−K_{σi}Vm`,去均值 | 奇异 pure-Neumann | 163 |
| **Sys3** | 立方体 `K_iso`,面 `i=0` 作 Dirichlet「界面」、其余 Neumann | 界面数据的提升,界面数据 = **本步 Sys2 解在该面上的迹** | 非奇异、病态 | 159 |

Sys3 的耦合是真的单向耦合:每步先解 Sys2,把它的迹当作 Sys3 的 Dirichlet 数据。

五种初值:cold / warm / **现行实现**(A-正交基 + 相对阈值 `1e-3·ref` + `erase(begin())`,与 `forward_ecg.cpp:2704-2705` 一致)/ 真滑窗 m=16 / 真滑窗 m=64。

---

## 2. 结果

**regime 0(快前沿 + 复极弥散,高秩)**

| 总迭代 vs cold | Sys1 | Sys2 | Sys3 |
|---|---|---|---|
| cold | 5616 | 57214 | 55607 |
| warm(上一步) | −27.8% | −28.0% | −27.2% |
| **现行实现 ★** | **−10.9%** | **+2.7%** | **±0.0%** |
| **真滑窗 m=16** | **−62.8%** | **−54.9%** | **−76.3%** |
| 真滑窗 m=64 | −59.9% | −54.2% | −75.9% |

**regime 2(低维快变,复现真机基准)**

| 总迭代 vs cold | Sys1 | Sys2 | Sys3 |
|---|---|---|---|
| cold | 5514 | 56890 | 50660 |
| warm | −2.5% | −2.1% | −1.9% |
| **现行实现 ★** | −67.9% | −76.8% | −69.2% |
| **真滑窗 m=16** | **−98.6%** | **−98.6%** | **−98.2%** |
| 真滑窗 m=64 | −98.8% | −99.0% | −98.5% |

---

## 3. Sys3:「封顶 ~10%」的结论要推翻

`RESEARCH_Sys3_acceleration_zh.md` §3.1 的结论是"初值类封顶 ~10%"(warm −9%、Fischer −11%),理由是"残差留在那 ~8 个慢的交界变化模里"。测试台上:

- 现行实现确实**接近 0**(regime 0 是 +0.03%,regime 2 是 −69.2% —— 后者只是因为相对阈值把基冻结在 4 个向量、根本没触发淘汰);
- **同一份数据、同一个窗口大小,把窗口改成"最近 m 个解"就是 −76.3% / −98.2%**。

**为什么 Sys3 反而是三个里最好的?** 因为躯干解是界面数据的**调和延拓**——体导体是个低通滤波器,前沿的细结构在体内被抹平。解轨迹的数值秩(88 个采样快照的 Gram 特征值):

| 相对阈值 | 1e-4 | 1e-6 | 1e-8 |
|---|---|---|---|
| Sys2 解 `u_e`(regime 0) | 10 | 18 | 34 |
| **Sys3 解 `u_T`(regime 0)** | **3** | **6** | **9** |

心脏里 34 维的东西,到躯干只剩 9 维 —— 一个 16 维窗口绰绰有余。

> 与 `-leadvol` 的"80 步张 ~54 维"不矛盾也不能互相替代:那是**真躯干几何 + 全场 13–16% 精度**的降基实验,这里是立方体模型问题,秩肯定被低估。但机理(调和延拓 = 低通)是真的,**值得在真机上直接量一次 Sys3 解快照的谱** —— 这个数就能直接回答"54 维到底是不是初值的障碍"。

---

## 4. Sys1:能做,但先修停机判据

`forward_ecg.cpp` 现在:`cg1.iterative_mode = false`(:688,连 warm 都没开)、`cg1.SetRelTol(1e-10)`。

**问题**:PETSc 默认判据是 `‖r_k‖ ≤ rtol·‖r_0‖`,`r_0` 依赖初值。初值越好 `r_0` 越小,**要下降的位数一点没变** ⇒ 在 rtol 判据下任何初值都省不了迭代。你们 Sys2 的代码正是用 `SetRelTol(0.0); SetAbsTol(1e-8*bn)`(:2547-2548)绕开的,Sys1 还没做。

改法(顺序不能反):

1. `cg1.SetRelTol(0); cg1.SetAbsTol(1e-10*‖b‖)` + `KSP_NORM_UNPRECONDITIONED` + `KSPSetInitialGuessNonzero`。
   **保持 1e-10**,别顺手改成 1e-8——那等于白送几次迭代,会污染对比。
2. 只开 warm(零存储、零 matvec):测试台上 **−27.8%**。
3. 窗口回收:测试台上 −62.8%(regime 0)/ −98.6%(regime 2)。

**但先算账**:Sys1 占三系统总迭代的 5616/118437 ≈ **5%**;维护一个窗口 ≈ 1 matvec + 2 归约 ≈ 1 次迭代 ≈ 16 次里的 6%。**结论:warm 免费拿 −28% 就够了,窗口回收对 Sys1 是最低优先级。**

---

## 5. 每个系统该配多大的窗口(m 扫描)

**先说结构**:三套窗口在代码里**本来就是分开的**,而且必须分开 —— `fisch_P`(Sys2,心脏 `nloc`)和 `f3_P`(Sys3,躯干 `Xt.Size()`)在不同网格上、长度不同、算子不同;Sys1 现在没有窗口。所以问题不是"要不要各自一套",而是**各自该配多大**。

扫 m ∈ {4,8,12,16,24,32,48,64}(同一个真滑窗,只改 m),同时用一个 flop 模型扣掉**维护窗口本身**的开销:

```
一次 CG 迭代  ≈ 1 matvec + 1 PC + 2 点积 + 3 axpy   ≈ 24 N
一步窗口维护  ≈ 2m 点积 + m axpy + 1 matvec         ≈ (6m+13) N
⇒ 窗口开销 ≈ (6m+13)/24 ≈ 0.25 m + 0.54 次迭代/步
```
(点积批成 O(1) 次归约,所以这个比值是**每个 rank 的本地 flop**,在任何规模上都成立。)

**regime 0(轨迹高秩)** —— 上行 = 实测降幅,下行 = 扣掉窗口开销后:

| | m=4 | m=8 | m=12 | m=16 | m=24 | m=32 | m=48 | m=64 |
|---|---|---|---|---|---|---|---|---|
| Sys1(每步 16 迭代) | **63.5** | 63.7 | 63.5 | 62.8 | 62.2 | 61.7 | 60.3 | 59.9 |
| | **53.9** | 47.8 | 41.4 | 34.5 | 21.5 | 8.4 | −17.9 | −43.2 |
| Sys2(每步 163) | **56.3** | 56.0 | 55.8 | 54.9 | 54.3 | 53.8 | 53.2 | 54.2 |
| | **55.4** | 54.5 | 53.8 | 52.2 | 50.4 | 48.8 | 45.8 | 44.5 |
| Sys3(每步 159) | 70.3 | **75.3** | 75.2 | 76.3 | 75.9 | 76.0 | 75.6 | 75.9 |
| | 69.3 | **73.8** | 73.0 | 73.4 | 71.8 | 70.6 | 67.7 | 65.5 |

**regime 2(轨迹 4 维)**:

| | m=4 | m=8 | m=12 | m=16 | m=24 | m=32 | m=48 | m=64 |
|---|---|---|---|---|---|---|---|---|
| Sys1 实测 / 扣开销 | 91.7 / **82.0** | 97.4 / 81.3 | 98.5 / 76.0 | 98.6 / 69.8 | 98.8 / 57.2 | 98.8 / 44.6 | 98.8 / 19.2 | 98.8 / −6.2 |
| Sys2 实测 / 扣开销 | 93.2 / 92.2 | 97.6 / 96.0 | 98.4 / **96.2** | 98.6 / 95.8 | 98.8 / 94.8 | 98.9 / 93.6 | 98.9 / 91.2 | 99.0 / 88.8 |
| Sys3 实测 / 扣开销 | 92.6 / 91.5 | 97.1 / 95.3 | 97.9 / **95.5** | 98.2 / 95.1 | 98.3 / 93.8 | 98.4 / 92.5 | 98.5 / 89.8 | 98.5 / 87.1 |

### 决定窗口大小的是两件事

1. **轨迹的有效维数** —— 窗口盖得住才有额外收益。Sys3 的轨迹是 9 维,所以 m 从 4 涨到 8–16 有明显提升(70.3 → 76.3);Sys2/Sys1 的轨迹是 34/29 维,**任何可行的窗口都盖不住**,于是窗口退化成一个"时间外插器",**4 个最近的解就够了**,再多纯属白拿开销。
2. **每步的迭代数** —— 决定开销占比。Sys1 每步只有 16 次迭代,m=16 的窗口就吃掉 4.5 次(28%),m=64 时维护比求解还贵(降幅变负)。Sys2/Sys3 每步 ~160 次,m=12 只占 2%,所以吃得起。

### 推荐值 vs 现在的代码

| | 现在 | 实测最优(扣开销) | 结论 |
|---|---|---|---|
| **Sys1** | 没有窗口 | **m = 4** | 加一个 4 向量的小窗口(比只用 warm 的 −27.8% 好很多);再大就亏 |
| **Sys2** | `FISCH_MAX = 16` | **m = 4–12** | 略偏大,但只值 1–2 个百分点,不急着改 |
| **Sys3** | `F3MAX = 12` | **m = 8–12** | **正好** |

**所以窗口大小本身并没有配错**(Sys3 的 12 是最优、Sys2 的 16 只大了一点点)——**错的是淘汰规则**(§3)。补丁修的正是后者;m 保持原值即可。

> 这一节取代了本报告早先"m=16 vs 64"的粗比较:那一版只测了两个点,得出的"大窗口更差是因为系数抵消(254–1300 倍)"仍然成立,但完整扫描给出了更有用的结论 —— **最优 m 很小,而且按系统不同**。

---

## 6. 补丁

`fischer_sliding_window.patch` 现在有 **6 个 hunk**,Sys2 三处 + Sys3 三处,`git apply --check` 对 `cardiac-sim-fakegeo` 干净通过:

| | Sys2 (`-fischer`) | Sys3 (`-fischer3`) |
|---|---|---|
| 声明 | :766 | :2239 |
| 投影 | :2564 | :2678 |
| 窗口更新 | :2586 | :2692 |

Sys3 那边**已经**用了相对阈值和干净的停机判据(`RelTol=0 + AbsTol=1e-8‖b‖` :2651、`KSP_NORM_UNPRECONDITIONED` :2298),所以只有淘汰规则要改;修完之后 Gram-Schmidt 整个消失,阈值也不用调了。

簿记(增量 Gram + 淘汰时的行列删除 + `gram_solve`)的单元测试 `fischer_gram_selftest.cpp` 对两处同样适用——两边是同一段代码。

---

## 6b. 三个解长什么样(`fig_fields_sys123.png`)

把三个系统的解直接画出来(中平面切片,regime 0 的三个时刻),回收好不好使的原因一眼可见:

| | 去极化前沿 t=8 ms | 平台段 t=90 ms | 复极前沿 t=250 ms |
|---|---|---|---|
| **Sys1 `V_m`** | 一条**尖锐的行波前沿**(几个网格宽) | 近似均匀(13–16 mV) | 复极前沿扫过 |
| **Sys2 `u_e`** | 由 `−K_{σi}V_m` 驱动,**保留了前沿的细结构** | 幅值掉到 ~0.5,形状仍随复极弥散变 | 再次出现锐利结构 |
| **Sys3 `u_T`** | 界面数据的**调和延拓**,离界面越远越平滑 | 相对变化只有 ±10% | 相对变化只有 ±4% |

> 画 Sys3 时把界面那一行(`i=0`)的值贴回去了 —— 解向量在 Dirichlet 节点上存的是 0,真实值由右端携带。

底部那张谱图是量化版本(88 个等间隔快照的 Gram 特征值,归一化):

| 相对阈值 | 1e-4 | 1e-6 | 1e-8 |
|---|---|---|---|
| Sys1 `V_m` | 10 | 14 | 29 |
| Sys2 `u_e` | 10 | 18 | **34** |
| **Sys3 `u_T`** | **3** | **6** | **9** |

**心脏里 34 维的东西,到躯干只剩 9 维** —— 体导体是低通滤波器,前沿的细结构在体内被抹平。
这就是为什么 m=16 的窗口对 Sys3 绰绰有余(−76.3%),而对 Sys2 只能盖住一部分(−54.9%):
**窗口只要盖住轨迹的有效维数,投影出的初值就直接落进容差。**

---

## 7. 诚实边界

- 模型问题:Jacobi 而非 bjacobi+ICC、串行、立方体不是真躯干(**Sys3 的秩很可能被低估**)。绝对数字不能搬。
- regime 2 的 −98% 带上界性质(4 维合成流形)。
- 测试台里 Sys3 的 warm/现行是 −27%/±0%,真机是 −9%/−11% —— 不完全对得上,说明工况刻画不完美。**能确定的是:现行实现在两个工况里都接近 0,而修好窗口在两个工况里都是几十个百分点。**

---

## 8. 下一步

1. **apply 补丁**,一次跑两个:`forward_ecg -fischer`(Sys2)和 `-fischer3`(Sys3),看 `[FISCHER]` / `[FISCHER3]` 汇总。
2. **Sys1**:先把停机判据从 `rtol` 改成 `atol=1e-10‖b‖` + 打开 warm;窗口回收先不做。
3. 真机上量一次 **Sys3 解快照的谱**(88 个快照的 Gram 特征值衰减),直接回答"54 维"是不是初值的障碍。
4. 硬步仍然归 deflation / 粗空间。

## 9. 文件

- 测试台:`fischer_sys13_test.c`(`./fischer_sys13_test 24 350 16 <regime> 12`)
- 数据:`fischer_sys13_regime0.csv`、`fischer_sys13_regime2.csv`
- 图:`fig_fischer_sys13.png`(`plot_fischer_sys13.py`)、`fig_fields_sys123.png`(`plot_fields_sys123.py`,输入 `fischer_sys13_slices_regime0.txt`)、`fig_window_sweep.png`(`plot_window_sweep.py`)
- 窗口扫描数据:`fischer_window_sweep_regime0.csv`、`fischer_window_sweep_regime2.csv`(`./fischer_sys13_test 24 350 16 <regime> 12 "" 1`)
- 补丁:`fischer_sliding_window.patch`(Sys2 + Sys3)
