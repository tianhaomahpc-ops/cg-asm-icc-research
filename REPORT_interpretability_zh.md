# sASM 的可解释性:逐图详解(信息传播 / 重叠重复计数 / 不精确放大)

最小算例(C+PETSc:`asm_bug_demo/infoprop.c`;绘图 `infoprop_out/plot_infoprop.py`),
把"因素 B = 重叠重复计数"、"信息传播"、"不精确被放大"变成可见的图。
**每张图都标注它对应哪个问题/设定。**

**三个设定(贯穿全文,务必对号入座)**:
- **1D / 2D-exact**(精确子域解,Cholesky):**对照组,没有不精确,因此没有反常**。
- **2D-ICC / 3D-ICC**(不精确子域解,ICC(0)):**反常发生的设定**——sASM 在这里才有用。
- **3D = 真实 Sys3**(1 面 Dirichlet + 5 面 Neumann Laplace,$n_x{=}48$,4 ranks);
  1D/2D 是把同一线性代数现象抽象出来的最小算例。

**总览迭代数**(CG 到 $\|r\|/\|b\|<10^{-8}$):

| 设定 | BASIC | sASM | 说明 |
|---|---|---|---|
| 1D 精确(=ICC,三对角无填充) | 15 | 25 | 无不精确 ⟹ 无反常;sASM 反而略差 |
| 2D 精确 | 23 | 27 | 同上 |
| **2D ICC(0)** | **102** | **67** | **反常+修复** |
| **3D ICC(0)**($O{=}2$,真实 Sys3) | **214** | **134** | **反常+修复(真实问题)** |
| 3D 精确 Cholesky($O{=}2$) | 38 | — | 对照:精确解很快,无反常 |

---

## 逐图详解

### Fig 1 `fig1_overcount.png` — 一次预条件 apply:重复计数可见
- **【对应问题】** 1D Laplace,8 子域,**精确解**(纯机制展示,与 ICC 无关)。
- **【图意】** 对一个局部 residual 施加 $\sum_iR_i^\top A_i^{-1}R_i$。上排是 multiplicity $m_k$
  (阴影=$m_k{=}2$ 的重叠区);下排是输出修正:**BASIC(红)在每条重叠缝处隆起**(被叠加 $m_k$ 次),
  **sASM(蓝)被 $1/\sqrt{m_k}$ 归一、保持平滑**。$O$ 越大,隆起越明显。
- **【结论】** 这是"因素 B = 重复计数"的**直接可视化**:$\sum_iR_i^\top R_i=\mathrm{diag}(m_k)$。
  但它本身无害——后面的图说明它何时才致命。

### Fig 2 `fig2_front.png` — 信息传播:为什么单层要 $O(\#\text{子域})$ 次迭代
- **【对应问题】** 1D Laplace,左边界 Dirichlet$=1$,**精确解**(传播是机制,与 BASIC/sASM 无关)。
- **【图意】** 椭圆问题全局耦合,但每次 Schwarz 只解局部 ⟹ **左边界的影响每步只前进约一个子域**
  (解从左侧逐步填进来)。前几步快(=overlap 的覆盖宽度),深部内区滞后。
- **【结论】** 单层固有代价 $O(\#\text{子域})$ 次迭代;那条**滞后的慢全局模正是两层法粗空间**要修的。
  overlap 越大、每步前进越远 ⟹ 迭代越少(这解释了"overlap 经典上有益")。**与 BASIC/sASM 无关。**

### Fig 3 `fig3_reshist2d.png` — 核心图:sASM 的收益只在不精确解时出现
- **【对应问题】** 2D Laplace,$4\times4$ 子域,$O{=}2$;**精确(实线)与 ICC(0)(虚线)对比**。
- **【图意】** 四条 CG 收敛曲线。
- **【结论】**
  - **实线(精确,$\omega{=}1$)**:BASIC(23)与 sASM(27)几乎重合——**过度计数本身不拖慢收敛**。
  - **虚线(ICC,$\omega{>}1$)**:BASIC **102** 明显变慢,sASM **67** 收回。
  - **一句话**:**去掉因素 B(sASM)只在因素 A(ICC 不精确)存在时值钱**——A×B 耦合的最干净证据。

### Fig 4 `fig4_heatmap2d.png` — 2D-ICC 残差场:缝处残差清得快慢
- **【对应问题】** 2D Laplace,$4\times4$,$O{=}2$,**ICC(0)**(反常设定)。
- **【图意】** 同样的 CG 步数 $k=5,15,30,60$,把 $|r|$ 画成热力图(对数色标)。
- **【结论】** 两者前期都在左侧+缝处堆残差;到 **$k{=}60$,BASIC 仍亮**(还需到 102),
  **sASM 已近乎清空**(67 收敛)。直观看到 sASM 把重叠区残差清得更快。

### Fig 5 `fig5_overlap_sweep.png` — 变 overlap:误差在缝处的占比随 overlap 增大
- **【对应问题】** 2D Laplace,$128^2$,$8\times8$ 子域,**ICC(0)**,overlap $O=1,2,4$;固定 $k{=}40$ 的残差场。
- **【图意】** 上排 BASIC、下排 sASM,三列 $O=1,2,4$。
- **【结论】**
  - **过度计数的缝区随 $O$ 变宽,被困在缝区的残差占比随之增大:$0.45\to0.51\to0.64$**
    (即"误差越来越集中在重叠缝处")。
  - sASM 在**每个 overlap** 下都更优(迭代 BASIC 182/195/185 vs sASM 131/130/129)。
  - **诚实说明**:2D 的迭代数随 $O$ **非单调**(因为 2D 的 ICC 不精确度本就较弱);
    "overlap↑ ⟹ 迭代↑"的**单调反常在 3D 才干净**(见 Fig 6)。

### Fig 6 `fig6_3d.png` — 真实 3D Sys3:反常与修复
- **【对应问题】** **真实 Sys3**(1 Dir + 5 Neu Laplace,$n_x{=}48$,4 ranks)。
- **【图意】** 左:$O{=}2$ 的 CG 残差史(BASIC 精确 38 / BASIC ICC 214 / sASM ICC 134);
  右:迭代数随 overlap(BASIC+ICC $132\to148\to179$,sASM $132\to104\to103$)。
- **【结论】**
  - **左**:ICC 下 BASIC 214 慢、sASM 134 修复;精确 Cholesky 38 很快(=无反常,对照)。
  - **右**:**这才是"误差随 overlap 越积越多"的干净版**——overlap 越大,BASIC+ICC 迭代越多
    (误差清得越慢);**sASM 把它反转成"越大越好"**。
  - 与 1D/2D 一致:**反常发生在 ICC(不精确)设定,精确解下无反常,sASM 恰好修不精确带来的放大。**

---

## 两个问题的答案(一句话各一)

**Q1 — inexact 为什么不好?**
抽象上界 $\kappa\le C_0^2\cdot\omega\cdot N_c$,**A(不精确 $\omega$)与 B(重复计数 $N_c$)相乘**。
精确解 $\omega{=}1$,过度计数只把 $\lambda_{\max}\le N_c$ 抬一下、CG 容忍(无反常);
ICC 时 $\omega{>}1$,BASIC 把**带误差的修正在重叠区叠加 $m_k$ 次**——误差累积、$\lambda_{\max}\le\omega N_c$、
且 overlap 越大 $\omega,N_c$ 越大 ⟹ 迭代随 overlap 上升(Fig 6 右),残差堆在缝处清不掉(Fig 4/5)。

**Q2 — sASM 为什么能克服?**
$\mathbf D^{-1/2}(\cdot)\mathbf D^{-1/2}$ 单位分解把重叠区计一次、**移除 $N_c$**($\lambda_{\max}\le\omega$),
**打断 A×B 相乘**——不精确修正不再被放大叠加,缝处不累积(Fig 4/5),收敛恢复(102→67、214→134)。
sASM **不修不精确本身**($\omega$ 仍 $>1$),只阻止过度计数去放大它——所以**恰好在子域解不精确时有用**,
精确时中性甚至略亏。信息每步只传约一个子域(Fig 2)是单层固有代价,与两法无关。
