# sASM 的可解释性:信息传播与重叠重复计数的可视化

用 1D/2D Laplace 的最小算例(C+PETSc:`asm_bug_demo/infoprop.c`;绘图
`asm_bug_demo/infoprop_out/plot_infoprop.py`),把抽象的"因素 B = 重叠重复计数"
和"信息传播"变成肉眼可见的图,从而解释 **sASM 到底在修什么、何时有用**。

- **离散**:1D P1/FD Laplace(256 unknowns,8 子域);2D 5-点 Laplace($64^2$,$4\times4$ 子域)。
- **子域解**:精确 Cholesky 与 ICC(0),均由 PETSc 完成;overlap / 限制 / $D^{-1/2}$ 权重显式控制。
- **图**:`fig1_overcount.png` … `fig4_heatmap2d.png`。

## 实测迭代数(同一算例)

| | 1D 精确 | 1D ICC | 2D 精确 | 2D ICC |
|---|---|---|---|---|
| BASIC | 15 | 15 | 23 | **102** |
| sASM | 25 | 25 | 27 | **67** |

**两条关键事实**:
1. **1D 的 ICC(0) 等于精确解**——一维三对角矩阵无填充,ICC(0) 即完整 Cholesky。
   所以 1D 没有因素 A,自然没有反常。
2. **sASM 只有在子域解不精确(2D ICC)时才更优**(67 vs 102);精确解下(1D 全部、2D 精确)
   sASM ≈ 甚至略差于 BASIC(过度计数本身不是瓶颈,$D^{-1/2}$ 再缩放还有小代价)。
   **这正是 A×B 耦合:去掉因素 B(sASM)只在因素 A 存在时见效。**

## 四张图讲的故事

**Fig 1 — 一次预条件 apply:重复计数看得见。**
对一个局部 residual 施加 $\sum_iR_i^\top A_i^{-1}R_i$:在重叠区($m_k{=}2$,图上阴影)
**BASIC(红)被叠加放大、在子域缝处隆起**;**sASM(蓝)被 $1/\sqrt{m_k}$ 归一、保持平滑**。
$O$ 越大,过计数区越宽、隆起越明显。这是因素 B 的直接可视化。

**Fig 2 — 信息传播:为什么单层要 $O(\#\text{子域})$ 次迭代。**
椭圆问题全局耦合,但每次 Schwarz 迭代只解局部问题 ⟹ **左边界的影响每步只前进约一个子域**
(图:解从左侧逐步"填进来")。前几步快(overlap 的覆盖宽度),深部内区滞后——
那条慢的全局模正是**两层法的粗空间**要解决的;单层只能为它付 $O(\#\text{子域})$ 次迭代。
overlap 越大,每步前进越远 ⟹ 迭代越少(经典的"overlap 有益")。

**Fig 3 — 2D residual 收敛史:sASM 的收益只在不精确解时出现(核心图)。**
- **实线(精确子域解)**:BASIC(23)与 sASM(27)几乎重合——过度计数本身不拖慢收敛。
- **虚线(ICC(0))**:BASIC **102** 步明显变慢,sASM **67** 步收回。
**把因素 B(sASM 去掉)只在因素 A(ICC 不精确)存在时值钱**——这张图把整个论点压成一幅。

**Fig 4 — 2D ICC residual 场(同迭代步对比):缝处残差被清得快慢。**
ICC(0) 下,把 $|r|$ 画成热力图(同样的 CG 步数 $k=5,15,30,60$):
两者前期都在左侧 + 缝处堆积残差;到 $k=60$,**BASIC 仍亮(还需到 102),sASM 已近乎清空**(67 收敛)。
直观看到 sASM 更快把重叠区的残差清干净。

## 结论(给"可解释性"的一句话)

> **BASIC 在重叠区把多个子域的修正叠加(过度计数,Fig 1)。这本身无害——精确子域解下被自动
> 控制(Fig 3 实线、1D 全部)。但当子域解不精确(ICC)时,过度计数的、带误差的修正在重叠区
> 互相放大、清不干净(Fig 4),拖慢收敛;sASM 用 $D^{-1/2}$ 单位分解把重复计数归一,
> 让不精确修正不再叠加放大,于是收敛恢复(Fig 3 虚线,102→67)。信息无论如何每步只传约一个
> 子域(Fig 2)——这是单层的固有代价,与 BASIC/sASM 无关。**

即 **sASM = "别把重叠区数两遍",它的价值恰好出现在"子域解不精确"这一前提下**——
这也解释了为什么我们的三维 ICC 算例反常显著、而精确-Cholesky 算例没有反常。
